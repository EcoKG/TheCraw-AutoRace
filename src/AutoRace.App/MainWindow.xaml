<Window x:Class="AutoRace.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AutoRace.App.ViewModels"
        mc:Ignorable="d"
        Title="AutoRace" Height="520" Width="900">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <TextBlock Text="AutoRace" FontSize="20" FontWeight="SemiBold" DockPanel.Dock="Left" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Content="Start" Command="{Binding StartCommand}" Margin="0,0,8,0" Padding="14,6" />
                <Button Content="Stop" Command="{Binding StopCommand}" Padding="14,6" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Margin="0,16,0,0" Padding="12" CornerRadius="8" BorderThickness="1" BorderBrush="#333333">
            <StackPanel>
                <TextBlock Text="Status" FontWeight="SemiBold" />
                <TextBlock Text="{Binding StatusText}" Margin="0,6,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
